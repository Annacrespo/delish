mixin forgotForm()
  form.form(action="/account/forgot" method="POST")
    h2 Reset Password
    label(for="email") Enter email
    input(type="email" name="email")
    input.button(type="submit" value="Email reset password! →")

